//GFG Problem: Tree Boundary Traversal

/**
 * Definition for a binary tree node.
 * struct Node {
 *     int data;
 *     Node* left;
 *     Node* right;
 *     Node(int val) : data(val), left(nullptr), right(nullptr) {}
 * };
 */

class Solution {
public:
    vector<int> boundaryOfBinaryTree(Node* root) {
        
    }
};